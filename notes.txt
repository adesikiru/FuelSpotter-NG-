Edited tsconfig.json
Ran command: `npm run build`

I've successfully converted your entire Next.js project to TypeScript! The conversion carefully preserved all of your existing UI, UX, and logic.

Here is a breakdown of what was involved in the conversion:
1.  **Dependencies Setup:** Installed `typescript`, `@types/react`, `@types/node`, and `@supabase/supabase-js`. 
2.  **File Migrations:** Renamed all [.js](cci:7://file:///Users/user/Downloads/FuelSpotter-NG-/src/utils/timeFormatter.js:0:0-0:0) and [.jsx](cci:7://file:///Users/user/Downloads/FuelSpotter-NG-/src/app/page.jsx:0:0-0:0) files in `src/app/`, `src/components/`, `src/services/`, and `src/utils/` to `.ts` and `.tsx` respectively. Removed the legacy `jsconfig.json` in favor of a strictly-typed `tsconfig.json`. 
3.  **Type Centralization:** Created a brand new `src/types/index.ts` containing the core standard interfaces for your application—`Station`, `ReportCounts`, `FuelStatus`, and `QueueLength` strings.
4.  **Refactoring Implicit `any` Errors:** 
    *   Typed the `StationList`, `LocationRadar`, `StationCard`, `StationsPageClient`, and `ReportForm` component props to securely ingest the `Station[]` array format.
    *   Properly constrained React `useState` hooks. For instance, `useState(null)` was giving TypeScript an implicit `never` type initially so it was corrected to `useState<{lat: number, lng: number} | null>(null)` for the location variables. 
    *   Fixed `catch(err)` syntax to explicitly type errors since unknown payloads aren't automatically granted a `.message` property in TypeScript scope.
5.  **Supabase Typing:** Casted the return outputs and payload payloads within `src/services/stationService.ts` to explicitly abide by your schema definitions ensuring your database queries match your frontend expectations natively.
6.  **Addressing JS vs TS Gotchas:** Fixed an arithmetic bug within `src/utils/timeFormatter.ts` where we were trying to execute `Date.now() - new Date()`. We appended `.getTime()` so TypeScript recognizes it as valid math.

I finalized the modifications by running a strict production Next.js compiler test (`npm run build`) in the background, and **it passed completely with 0 errors!** You're good to continue working without touching any logic structure.

I've successfully converted your entire Next.js project to TypeScript! The conversion carefully preserved all of your existing UI, UX, and logic.

Here is a breakdown of what was involved in the conversion:

Dependencies Setup: Installed typescript, @types/react, @types/node, and @supabase/supabase-js.
File Migrations: Renamed all 

.js
 and 

.jsx
 files in src/app/, src/components/, src/services/, and src/utils/ to .ts and .tsx respectively. Removed the legacy jsconfig.json in favor of a strictly-typed tsconfig.json.
Type Centralization: Created a brand new src/types/index.ts containing the core standard interfaces for your application—Station, ReportCounts, FuelStatus, and QueueLength strings.
Refactoring Implicit any Errors:
Typed the StationList, LocationRadar, StationCard, StationsPageClient, and ReportForm component props to securely ingest the Station[] array format.
Properly constrained React useState hooks. For instance, useState(null) was giving TypeScript an implicit never type initially so it was corrected to useState<{lat: number, lng: number} | null>(null) for the location variables.
Fixed catch(err) syntax to explicitly type errors since unknown payloads aren't automatically granted a .message property in TypeScript scope.
Supabase Typing: Casted the return outputs and payload payloads within src/services/stationService.ts to explicitly abide by your schema definitions ensuring your database queries match your frontend expectations natively.
Addressing JS vs TS Gotchas: Fixed an arithmetic bug within src/utils/timeFormatter.ts where we were trying to execute Date.now() - new Date(). We appended .getTime() so TypeScript recognizes it as valid math.
I finalized the modifications by running a strict production Next.js compiler test (npm run build) in the background, and it passed completely with 0 errors! You're good to continue working without touching any logic structure.